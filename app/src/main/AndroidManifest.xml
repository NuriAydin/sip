<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nuri.proje">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Sınıf İçi Paylaşım"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".uploadingActivity"></activity>
        <activity android:name=".uploadActivity" />
        <activity android:name=".TEMPActivity" />
        <activity android:name=".yukleniyorActivity" />
        <activity android:name=".giris_kayitActivity" />
        <activity android:name=".kullanicilarActivity" />
        <activity android:name=".onayActivity" />
        <activity android:name=".chatActivity" />
        <activity android:name=".sifremi_unuttumActivity" />
        <activity android:name=".secimActivity" />
        <activity android:name=".kayitActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>